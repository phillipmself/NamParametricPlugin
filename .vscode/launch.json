{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (CMake Launch Target)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
